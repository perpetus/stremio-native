import { Theme, IconData } from "theme.slint";
import { AppActions, AppState } from "app_contract.slint";
import { MediaCardItem } from "components/media_card.slint";
import { SearchSuggestion } from "components/search.slint";
import { BoardSection } from "pages/board.slint";
import { DiscoverRow } from "pages/discover.slint";
import { LibraryRow } from "pages/library.slint";
import { AddonItem } from "pages/addons.slint";
import { StreamLink, EpisodeItem } from "pages/details.slint";
import { CalendarRow, CalendarCell, CalendarMediaItem } from "pages/calendar.slint";
import { AppDebugMenu, DebugPageOption, DebugStateOption } from "components/app_debug_menu.slint";
import { UiStyle } from "cinematic/types.slint";
import { CinematicTheme } from "cinematic/theme.slint";
import { CinematicRoutes } from "cinematic/routes.slint";
import { AuthLayer } from "layers/auth_layer.slint";
import { DetailsLayer } from "layers/details_layer.slint";
import { PlayerLayer } from "layers/player_layer.slint";
import { ClassicShell } from "shells/classic_shell.slint";
import { PlayerVideoSurfaceMode } from "rendering_types.slint";

import { Fonts } from "imported_fonts.slint";
import "./assets/fonts/PlusJakartaSans.ttf";

export { Theme, IconData, UiStyle, PlayerVideoSurfaceMode, AppActions, MediaCardItem, StreamLink, EpisodeItem, BoardSection, DiscoverRow, LibraryRow, AddonItem, CalendarRow, CalendarCell, CalendarMediaItem, SearchSuggestion }

export component MainWindow inherits Window {
    in-out property profile-menu-open <=> AppState.profile-menu-open;
    in-out property ui-style <=> AppState.ui-style;
    // Native shortcut handling reads these presentation signals so editable
    // controls retain standard keyboard behavior.
    out property <bool> application-shortcuts-focused;
    out property <bool> shortcut-player-active;
    in-out property settings-active-category <=> AppState.settings-active-category;
    in-out property debug-preview-enabled <=> AppState.debug-preview-enabled;
    in-out property debug-menu-open <=> AppState.debug-menu-open;
    in property debug-missing-artwork-path <=> AppState.debug-missing-artwork-path;
    in property debug-show-missing-artwork <=> AppState.debug-show-missing-artwork;
    in-out property debug-preview-page <=> AppState.debug-preview-page;
    in-out property debug-preview-state <=> AppState.debug-preview-state;
    in property active-tab <=> AppState.active-tab;
    in-out property username <=> AppState.username;
    in-out property avatar-letter <=> AppState.avatar-letter;
    in-out property avatar-image <=> AppState.avatar-image;
    in-out property has-avatar-image <=> AppState.has-avatar-image;
    in property default-avatar-icon <=> AppState.default-avatar-icon;
    in property anonymous-icon <=> AppState.anonymous-icon;
    in-out property loading <=> AppState.loading;
    in-out property error-message <=> AppState.error-message;
    in-out property server-status <=> AppState.server-status;
    in-out property server-url <=> AppState.server-url;
    in-out property board-continue-watching <=> AppState.board-continue-watching;
    in-out property board-sections <=> AppState.board-sections;
    in-out property search-sections <=> AppState.search-sections;
    in-out property search-suggestions <=> AppState.search-suggestions;
    in-out property search-query <=> AppState.search-query;
    in-out property search-loading <=> AppState.search-loading;
    in-out property search-catalog-count <=> AppState.search-catalog-count;
    in-out property discover-rows <=> AppState.discover-rows;
    in-out property discover-column-count <=> AppState.discover-column-count;
    in property discover-types <=> AppState.discover-types;
    in-out property discover-active-type <=> AppState.discover-active-type;
    in property discover-catalogs <=> AppState.discover-catalogs;
    in-out property discover-active-catalog <=> AppState.discover-active-catalog;
    in property discover-genres <=> AppState.discover-genres;
    in-out property discover-active-genre <=> AppState.discover-active-genre;
    in-out property discover-scroll-y <=> AppState.discover-scroll-y;
    in-out property discover-has-preview <=> AppState.discover-has-preview;
    in-out property discover-preview-title <=> AppState.discover-preview-title;
    in-out property discover-preview-description <=> AppState.discover-preview-description;
    in-out property discover-preview-rating <=> AppState.discover-preview-rating;
    in-out property discover-preview-year <=> AppState.discover-preview-year;
    in-out property discover-preview-runtime <=> AppState.discover-preview-runtime;
    in-out property discover-preview-genres <=> AppState.discover-preview-genres;
    in-out property discover-preview-cast <=> AppState.discover-preview-cast;
    in-out property discover-preview-directors <=> AppState.discover-preview-directors;
    in-out property discover-preview-is-in-library <=> AppState.discover-preview-is-in-library;
    in-out property discover-preview-is-watched <=> AppState.discover-preview-is-watched;
    in-out property discover-preview-has-trailer <=> AppState.discover-preview-has-trailer;
    in-out property discover-preview-poster <=> AppState.discover-preview-poster;
    in-out property discover-preview-poster-url <=> AppState.discover-preview-poster-url;
    in-out property library-rows <=> AppState.library-rows;
    in-out property library-column-count <=> AppState.library-column-count;
    in-out property library-active-type <=> AppState.library-active-type;
    in-out property library-active-sort <=> AppState.library-active-sort;
    in-out property library-scroll-y <=> AppState.library-scroll-y;
    in-out property addons-list <=> AppState.addons-list;
    in-out property addons-search-query <=> AppState.addons-search-query;
    in-out property addons-active-filter <=> AppState.addons-active-filter;
    in-out property addons-active-type <=> AppState.addons-active-type;
    in property addon-details-open <=> AppState.addon-details-open;
    in-out property addon-details-addon <=> AppState.addon-details-addon;
    in-out property addon-details-loading <=> AppState.addon-details-loading;
    in-out property addon-details-error <=> AppState.addon-details-error;
    in-out property addon-details-configurable <=> AppState.addon-details-configurable;
    in-out property addon-details-configuration-required <=> AppState.addon-details-configuration-required;
    in-out property detail-title <=> AppState.detail-title;
    in-out property detail-description <=> AppState.detail-description;
    in-out property detail-poster <=> AppState.detail-poster;
    in-out property detail-poster-url <=> AppState.detail-poster-url;
    in-out property detail-background <=> AppState.detail-background;
    in-out property detail-background-url <=> AppState.detail-background-url;
    in-out property detail-year <=> AppState.detail-year;
    in-out property detail-runtime <=> AppState.detail-runtime;
    in-out property detail-rating <=> AppState.detail-rating;
    in-out property detail-genres <=> AppState.detail-genres;
    in-out property detail-cast <=> AppState.detail-cast;
    in-out property detail-directors <=> AppState.detail-directors;
    in-out property detail-is-watched <=> AppState.detail-is-watched;
    in-out property detail-is-in-library <=> AppState.detail-is-in-library;
    in-out property detail-has-trailer <=> AppState.detail-has-trailer;
    in-out property detail-trailer-selection-id <=> AppState.detail-trailer-selection-id;
    in property show-details <=> AppState.show-details;
    in-out property details-loading <=> AppState.details-loading;
    in-out property stream-links <=> AppState.stream-links;
    in-out property detail-stream-providers <=> AppState.detail-stream-providers;
    in-out property detail-stream-loading-count <=> AppState.detail-stream-loading-count;
    in-out property detail-active-stream-provider <=> AppState.detail-active-stream-provider;
    in-out property detail-stream-filter-open <=> AppState.detail-stream-filter-open;
    in-out property detail-is-series <=> AppState.detail-is-series;
    in-out property detail-seasons <=> AppState.detail-seasons;
    in-out property detail-active-season <=> AppState.detail-active-season;
    in-out property detail-episode-names <=> AppState.detail-episode-names;
    in-out property detail-active-episode-idx <=> AppState.detail-active-episode-idx;
    in-out property detail-episodes <=> AppState.detail-episodes;
    in-out property detail-in-stream-view <=> AppState.detail-in-stream-view;
    in-out property detail-episode-search-query <=> AppState.detail-episode-search-query;
    in-out property calendar-selected-year <=> AppState.calendar-selected-year;
    in-out property calendar-selected-month <=> AppState.calendar-selected-month;
    in-out property calendar-previous-month <=> AppState.calendar-previous-month;
    in-out property calendar-next-month <=> AppState.calendar-next-month;
    in-out property calendar-rows <=> AppState.calendar-rows;
    in-out property calendar-loading <=> AppState.calendar-loading;
    in property show-player <=> AppState.show-player;
    in-out property <PlayerVideoSurfaceMode> player-video-surface-mode: PlayerVideoSurfaceMode.image;
    in-out property player-title <=> AppState.player-title;
    in-out property player-stream-name <=> AppState.player-stream-name;
    in-out property player-poster-image <=> AppState.player-poster-image;
    in-out property player-video-frame <=> AppState.player-video-frame;
    in-out property player-has-video-frame <=> AppState.player-has-video-frame;
    in-out property player-loading <=> AppState.player-loading;
    in-out property player-buffering <=> AppState.player-buffering;
    in-out property player-buffering-percent <=> AppState.player-buffering-percent;
    in-out property player-paused <=> AppState.player-paused;
    in-out property player-volume <=> AppState.player-volume;
    in-out property player-muted <=> AppState.player-muted;
    in-out property player-progress <=> AppState.player-progress;
    in-out property player-elapsed-time-str <=> AppState.player-elapsed-time-str;
    in-out property player-total-time-str <=> AppState.player-total-time-str;
    in-out property player-subtitles-tracks <=> AppState.player-subtitles-tracks;
    in-out property player-subtitle-track-languages <=> AppState.player-subtitle-track-languages;
    in-out property player-subtitle-track-origins <=> AppState.player-subtitle-track-origins;
    in-out property player-subtitle-languages <=> AppState.player-subtitle-languages;
    in-out property player-subtitle-language-track-indices <=> AppState.player-subtitle-language-track-indices;
    in-out property player-audio-tracks <=> AppState.player-audio-tracks;
    in-out property player-audio-track-languages <=> AppState.player-audio-track-languages;
    in-out property player-audio-track-labels <=> AppState.player-audio-track-labels;
    in-out property player-active-subtitle-idx <=> AppState.player-active-subtitle-idx;
    in-out property player-active-audio-idx <=> AppState.player-active-audio-idx;
    in-out property player-subtitle-delay-seconds <=> AppState.player-subtitle-delay-seconds;
    in-out property player-subtitle-size-percent <=> AppState.player-subtitle-size-percent;
    in-out property player-subtitle-offset-percent <=> AppState.player-subtitle-offset-percent;
    in-out property player-is-series <=> AppState.player-is-series;
    in-out property player-episode-names <=> AppState.player-episode-names;
    in-out property player-active-episode-idx <=> AppState.player-active-episode-idx;
    in-out property player-playback-speed <=> AppState.player-playback-speed;
    in-out property player-video-scale-mode <=> AppState.player-video-scale-mode;
    in-out property player-error <=> AppState.player-error;
    in-out property player-video-format <=> AppState.player-video-format;
    in-out property player-audio-format <=> AppState.player-audio-format;
    in-out property player-file-format <=> AppState.player-file-format;
    in-out property player-hwdec <=> AppState.player-hwdec;
    in-out property player-buffered-percent <=> AppState.player-buffered-percent;
    in-out property player-download-speed <=> AppState.player-download-speed;
    in-out property player-upload-speed <=> AppState.player-upload-speed;
    in-out property player-peer-count <=> AppState.player-peer-count;
    in-out property player-stream-progress <=> AppState.player-stream-progress;
    in-out property player-seek-step-seconds <=> AppState.player-seek-step-seconds;
    in-out property player-short-seek-step-seconds <=> AppState.player-short-seek-step-seconds;
    in-out property player-show-skip-button <=> AppState.player-show-skip-button;
    in-out property player-skip-button-label <=> AppState.player-skip-button-label;
    in-out property player-controls-visible <=> AppState.player-controls-visible;
    in-out property player-show-subtitles-menu <=> AppState.player-show-subtitles-menu;
    in-out property player-show-audio-menu <=> AppState.player-show-audio-menu;
    in-out property player-show-speed-menu <=> AppState.player-show-speed-menu;
    in-out property player-show-stats-menu <=> AppState.player-show-stats-menu;
    in-out property player-show-options-menu <=> AppState.player-show-options-menu;
    in-out property player-show-playlist-drawer <=> AppState.player-show-playlist-drawer;
    in-out property player-show-context-menu <=> AppState.player-show-context-menu;
    in-out property torrent-cache-size <=> AppState.torrent-cache-size;
    in-out property settings-interface-language <=> AppState.settings-interface-language;
    in-out property settings-subtitles-language <=> AppState.settings-subtitles-language;
    in-out property settings-torrent-profile <=> AppState.settings-torrent-profile;
    in-out property settings-hardware-acceleration <=> AppState.settings-hardware-acceleration;
    in-out property settings-binge-watching <=> AppState.settings-binge-watching;
    in-out property settings-discord-rpc-enabled <=> AppState.settings-discord-rpc-enabled;
    in-out property settings-tidb-api-key <=> AppState.settings-tidb-api-key;
    in-out property settings-tidb-show-intro <=> AppState.settings-tidb-show-intro;
    in-out property settings-tidb-show-recap <=> AppState.settings-tidb-show-recap;
    in-out property settings-tidb-show-credits <=> AppState.settings-tidb-show-credits;
    in-out property settings-tidb-show-preview <=> AppState.settings-tidb-show-preview;
    in-out property settings-hide-spoilers <=> AppState.settings-hide-spoilers;
    in-out property settings-gamepad-support <=> AppState.settings-gamepad-support;
    in-out property settings-play-in-background <=> AppState.settings-play-in-background;
    in-out property settings-subtitles-auto-select <=> AppState.settings-subtitles-auto-select;
    in-out property settings-subtitles-font <=> AppState.settings-subtitles-font;
    in-out property settings-subtitles-size <=> AppState.settings-subtitles-size;
    in-out property settings-subtitles-bold <=> AppState.settings-subtitles-bold;
    in-out property settings-subtitles-offset <=> AppState.settings-subtitles-offset;
    in-out property settings-seek-duration <=> AppState.settings-seek-duration;
    in-out property settings-pause-on-minimize <=> AppState.settings-pause-on-minimize;
    in-out property settings-quit-on-close <=> AppState.settings-quit-on-close;
    in-out property settings-export-loading <=> AppState.settings-export-loading;
    in-out property settings-export-status <=> AppState.settings-export-status;
    in-out property settings-streaming-seeding <=> AppState.settings-streaming-seeding;
    in-out property settings-streaming-dht <=> AppState.settings-streaming-dht;
    in-out property settings-streaming-pex <=> AppState.settings-streaming-pex;
    in-out property settings-streaming-lsd <=> AppState.settings-streaming-lsd;
    in-out property settings-streaming-remote-url <=> AppState.settings-streaming-remote-url;
    in-out property settings-streaming-logs-text <=> AppState.settings-streaming-logs-text;
    in property board-icon <=> AppState.board-icon;
    in property discover-icon <=> AppState.discover-icon;
    in property library-icon <=> AppState.library-icon;
    in property calendar-icon <=> AppState.calendar-icon;
    in property addons-icon <=> AppState.addons-icon;
    in property settings-icon <=> AppState.settings-icon;
    in property logout-icon <=> AppState.logout-icon;
    in property mail-icon <=> AppState.mail-icon;
    in property lock-icon <=> AppState.lock-icon;
    in property search-icon <=> AppState.search-icon;
    in property facebook-icon <=> AppState.facebook-icon;
    in property apple-icon <=> AppState.apple-icon;
    in property icon-play <=> AppState.icon-play;
    in property icon-pause <=> AppState.icon-pause;
    in property icon-next <=> AppState.icon-next;
    in property icon-volume-high <=> AppState.icon-volume-high;
    in property icon-volume-low <=> AppState.icon-volume-low;
    in property icon-volume-mute <=> AppState.icon-volume-mute;
    in property icon-fullscreen <=> AppState.icon-fullscreen;
    in property icon-subtitles <=> AppState.icon-subtitles;
    in property icon-audio <=> AppState.icon-audio;
    in property icon-speed <=> AppState.icon-speed;
    in property icon-stats <=> AppState.icon-stats;
    in property icon-options <=> AppState.icon-options;
    in property icon-menu <=> AppState.icon-menu;
    in property icon-back <=> AppState.icon-back;
    in property icon-link <=> AppState.icon-link;
    in property icon-magnet <=> AppState.icon-magnet;
    in property icon-download <=> AppState.icon-download;
    in property icon-eye <=> AppState.icon-eye;
    in property icon-eye-off <=> AppState.icon-eye-off;
    in property icon-clapperboard <=> AppState.icon-clapperboard;
    in property refresh-icon <=> AppState.refresh-icon;
    in property folder-icon <=> AppState.folder-icon;
    in-out property is-fullscreen <=> AppState.is-fullscreen;
    callback toggle-fullscreen;
    callback open-external-url(string);
    callback submit-login(string, string);
    callback submit-register(string, string, bool, bool, bool);
    callback facebook-login;
    callback apple-login;
    callback cancel-auth;
    callback request-password-reset(string);
    callback guest-login;
    callback logout;
    callback board-item-selected(string, string, string);
    callback remove-continue-watching(string);
    callback board-see-all-continue-watching;
    callback board-see-all-clicked(string, string, string);
    callback global-search-edited(string);
    callback global-search-submitted(string);
    callback global-search-suggestion-selected(string, string);
    callback search-item-selected(string, string, string);
    callback request-poster(string);
    callback discover-type-changed(string);
    callback discover-catalog-changed(string);
    callback discover-genre-changed(string);
    callback discover-search-changed(string);
    callback discover-item-selected(string);
    callback play-preview;
    callback toggle-library-preview;
    callback library-type-changed(string);
    callback library-sort-changed(string);
    callback library-search-changed(string);
    callback library-item-selected(string, string);
    callback install-addon(string);
    callback uninstall-addon(string);
    callback open-addon-details(string);
    callback close-addon-details;
    callback configure-addon(string);
    callback share-addon(string);
    callback addons-search-changed(string);
    callback details-toggle-library;
    callback details-toggle-watched;
    callback details-season-changed(int);
    callback details-episode-changed(int);
    callback details-episode-search-changed(string);
    callback details-toggle-episode-watched(string);
    callback details-copy-stream-link(string);
    callback details-copy-magnet-link(string);
    callback details-copy-download-link(string);
    callback details-toggle-season-watched(int);
    callback play-stream(string);
    callback apply-cache-settings(float);
    callback settings-change-interface-language(string);
    callback settings-change-ui-style(UiStyle);
    callback settings-change-subtitles-language(string);
    callback settings-change-torrent-profile(string);
    callback settings-change-hardware-acceleration(bool);
    callback settings-clear-search-history;
    callback shutdown-server;
    callback settings-change-seeding-enabled(bool);
    callback settings-change-dht-enabled(bool);
    callback settings-change-pex-enabled(bool);
    callback settings-change-lsd-enabled(bool);
    callback settings-refresh-logs;
    callback settings-open-logs-folder;
    callback settings-export-data;
    callback settings-change-binge-watching(bool);
    callback settings-change-discord-rpc-enabled(bool);
    callback settings-change-tidb-api-key(string);
    callback settings-change-tidb-show-intro(bool);
    callback settings-change-tidb-show-recap(bool);
    callback settings-change-tidb-show-credits(bool);
    callback settings-change-tidb-show-preview(bool);
    callback settings-change-hide-spoilers(bool);
    callback settings-change-gamepad-support(bool);
    callback settings-change-play-in-background(bool);
    callback settings-change-subtitles-auto-select(bool);
    callback settings-change-subtitles-font(string);
    callback settings-change-subtitles-size(string);
    callback settings-change-subtitles-bold(bool);
    callback settings-change-subtitles-offset(string);
    callback settings-change-seek-duration(string);
    callback settings-change-pause-on-minimize(bool);
    callback settings-change-quit-on-close(bool);
    callback tab-changed(int);
    callback navigation-back;
    callback navigation-forward;
    callback calendar-previous;
    callback calendar-next;
    callback calendar-item-selected(string, string);
    callback player-toggle-pause;
    callback player-seek(float);
    callback player-seek-relative(float);
    callback player-change-volume(float);
    callback player-toggle-mute;
    callback player-change-subtitle(int);
    callback player-change-audio(int);
    callback player-skip-segment;
    callback player-change-subtitle-delay(float);
    callback player-change-subtitle-size(float);
    callback player-change-subtitle-offset(float);
    callback player-change-speed(float);
    callback player-change-video-scale(int);
    callback player-copy-stream-link;
    callback player-download-video;
    callback player-play-episode(int);
    callback player-toggle-fullscreen;
    callback player-close;
    callback player-activity;
    callback player-surface-state-changed(bool, PlayerVideoSurfaceMode);
    title: "Stremio";
    preferred-width: 1280px;
    preferred-height: 720px;
    min-width: 1000px;
    min-height: 600px;
    background: root.effective-show-player
        && root.player-video-surface-mode == PlayerVideoSurfaceMode.native-window
        ? transparent : Theme.background;
    default-font-family: "Plus Jakarta Sans";
    forward-focus: app-shortcuts;
    property <length> ui-unit: root.width >= 2800px ? 18px :
        (root.width >= 2200px ? 16px : (root.width >= 1600px ? 15px : 14px));
    // Disabled by default. `slint-viewer --load-data` can enable this harness
    // without replacing MainWindow, so every state uses the production shell,
    // routing bounds, overlays, and page components.
    // A string rather than an image keeps the intentional failure isolated:
    // invalid image values would reject the entire repeated fixture model.
    private property <[DebugPageOption]> debug-pages: [
        { label: "Auth", page: -1 },
        { label: "Board", page: 0 },
        { label: "Discover", page: 1 },
        { label: "Library", page: 2 },
        { label: "Addons", page: 3 },
        { label: "Settings", page: 4 },
        { label: "Calendar", page: 5 },
        { label: "Search", page: 6 },
        { label: "Details", page: 7 },
        { label: "Player", page: 8 }
    ];
    private property <[DebugStateOption]> debug-auth-states: [
        { label: "Login", value: "login" },
        { label: "Register", value: "register" },
        { label: "Loading", value: "auth-loading" },
        { label: "Invalid credentials", value: "auth-error" }
    ];
    private property <[DebugStateOption]> debug-board-states: [
        { label: "Loaded", value: "loaded" },
        { label: "Loading", value: "loading" },
        { label: "Empty", value: "empty" },
        { label: "Continue watching only", value: "continue-only" },
        { label: "Catalogs only", value: "catalogs-only" },
        { label: "Hero fallback", value: "hero-fallback" },
        { label: "Rail focus start", value: "rail-focus-start" },
        { label: "Rail focus middle", value: "rail-focus-middle" },
        { label: "Rail focus end", value: "rail-focus-end" },
        { label: "Profile menu", value: "profile-menu" }
    ];
    private property <[DebugStateOption]> debug-discover-states: [
        { label: "Catalog", value: "catalog" },
        { label: "Loading", value: "loading" },
        { label: "Empty", value: "empty" },
        { label: "Movie selected", value: "movie-selected" },
        { label: "Series selected", value: "series-selected" },
        { label: "Type dropdown", value: "type-dropdown" },
        { label: "Catalog dropdown", value: "catalog-dropdown" },
        { label: "Genre dropdown", value: "genre-dropdown" }
    ];
    private property <[DebugStateOption]> debug-library-states: [
        { label: "Loaded", value: "loaded" },
        { label: "Loading", value: "loading" },
        { label: "Empty", value: "empty" },
        { label: "Type dropdown", value: "type-dropdown" },
        { label: "Sort dropdown", value: "sort-dropdown" }
    ];
    private property <[DebugStateOption]> debug-addons-states: [
        { label: "Loaded", value: "loaded" },
        { label: "Loading", value: "loading" },
        { label: "Empty", value: "empty" },
        { label: "Addon details", value: "addon-details" },
        { label: "Details loading", value: "addon-details-loading" },
        { label: "Details error", value: "addon-details-error" },
        { label: "Filter dropdown", value: "filter-dropdown" },
        { label: "Type dropdown", value: "type-dropdown" },
        { label: "Add addon dialog", value: "add-addon" }
    ];
    private property <[DebugStateOption]> debug-settings-states: [
        { label: "General", value: "general" },
        { label: "Interface", value: "interface" },
        { label: "Player", value: "player-settings" },
        { label: "Streaming", value: "streaming" },
        { label: "Shortcuts", value: "shortcuts" },
        { label: "Diagnostics", value: "diagnostics" },
        { label: "Exporting", value: "exporting" }
    ];
    private property <[DebugStateOption]> debug-calendar-states: [
        { label: "Month loaded", value: "loaded" },
        { label: "Loading", value: "loading" },
        { label: "Signed out", value: "signed-out" },
        { label: "Empty month", value: "empty" }
    ];
    private property <[DebugStateOption]> debug-search-states: [
        { label: "Results", value: "results" },
        { label: "Loading", value: "loading" },
        { label: "No results", value: "empty" },
        { label: "No query", value: "idle" }
    ];
    private property <[DebugStateOption]> debug-details-states: [
        { label: "Loading title", value: "details-loading" },
        { label: "Movie streams", value: "movie-streams" },
        { label: "Movie no streams", value: "movie-no-streams" },
        { label: "Movie filter open", value: "movie-filter" },
        { label: "Series episodes", value: "series-episodes" },
        { label: "Season dropdown", value: "series-season-menu" },
        { label: "Selected episode streams", value: "series-streams" },
        { label: "Streams still loading", value: "series-stream-loading" },
        { label: "Episode no streams", value: "series-no-streams" }
    ];
    private property <[DebugStateOption]> debug-player-states: [
        { label: "Loading", value: "player-loading" },
        { label: "Buffering", value: "buffering" },
        { label: "Paused controls", value: "paused" },
        { label: "Playing controls", value: "playing" },
        { label: "Skip intro", value: "skip-intro" },
        { label: "Subtitle menu", value: "subtitles" },
        { label: "Audio menu", value: "audio" },
        { label: "Speed menu", value: "speed" },
        { label: "Playback info", value: "stats" },
        { label: "More options", value: "options" },
        { label: "Context menu", value: "context-menu" },
        { label: "Episode drawer", value: "episodes" },
        { label: "Playback error", value: "player-error" },
        { label: "Controls hidden", value: "controls-hidden" }
    ];
    private property <[DebugStateOption]> debug-state-options:
        root.debug-preview-page == -1 ? root.debug-auth-states :
        (root.debug-preview-page == 0 ? root.debug-board-states :
        (root.debug-preview-page == 1 ? root.debug-discover-states :
        (root.debug-preview-page == 2 ? root.debug-library-states :
        (root.debug-preview-page == 3 ? root.debug-addons-states :
        (root.debug-preview-page == 4 ? root.debug-settings-states :
        (root.debug-preview-page == 5 ? root.debug-calendar-states :
        (root.debug-preview-page == 6 ? root.debug-search-states :
        (root.debug-preview-page == 7 ? root.debug-details-states : root.debug-player-states))))))));

    private property <int> effective-active-tab: root.debug-preview-enabled
        ? (root.debug-preview-page >= 0 && root.debug-preview-page <= 6 ? root.debug-preview-page : 0)
        : root.active-tab;
    private property <bool> effective-show-details: root.debug-preview-enabled
        ? root.debug-preview-page == 7
        : root.show-details;
    private property <bool> effective-show-player: root.debug-preview-enabled
        ? root.debug-preview-page == 8
        : root.show-player;
    private property <bool> show-auth-page: root.debug-preview-enabled
        ? root.debug-preview-page == -1
        : root.username == "";
    private property <bool> show-main-shell: root.debug-preview-enabled
        ? root.debug-preview-page >= 0 && root.debug-preview-page <= 6
        : root.username != "";

    // Rust owns shortcut policy. This focus scope only tells the native
    // handler when no editable or transient control has captured the keyboard.
    application-shortcuts-focused: app-shortcuts.has-focus;
    shortcut-player-active: root.effective-show-player;

    changed effective-show-player => {
        if (root.effective-show-player) {
            app-shortcuts.focus();
        }
        root.player-surface-state-changed(
            root.effective-show-player,
            root.player-video-surface-mode,
        );
    }

    changed player-video-surface-mode => {
        root.player-surface-state-changed(
            root.effective-show-player,
            root.player-video-surface-mode,
        );
    }

    app-shortcuts := FocusScope {}

    // Visual layout routing
    Rectangle {
        x: 0px;
        y: 0px;
        width: 100%;
        height: 100%;
        background: root.effective-show-player
            && root.player-video-surface-mode == PlayerVideoSurfaceMode.native-window
            ? transparent
            : (root.ui-style == UiStyle.cinematic ? CinematicTheme.canvas
                : (root.show-auth-page ? Theme.background : Theme.main-gradient));

        AuthLayer {
            width: 100%;
            height: 100%;
            show-auth-layer: root.show-auth-page && !root.effective-show-player;
        }

        if (root.show-main-shell && !root.effective-show-player
            && root.ui-style == UiStyle.classic) : ClassicShell {
            width: 100%;
            height: 100%;
            route-index: root.effective-active-tab;
            show-details-layer: root.effective-show-details;
            settings-active-category <=> root.settings-active-category;
        }

        if (root.show-main-shell && !root.effective-show-player
            && root.ui-style == UiStyle.cinematic
            && !root.effective-show-details) : CinematicRoutes {
            width: 100%;
            height: 100%;
            route-index: root.effective-active-tab;
            settings-active-category <=> root.settings-active-category;
        }

        DetailsLayer {
            width: 100%;
            height: 100%;
            show-details-layer: root.effective-show-details && !root.effective-show-player;
        }

        PlayerLayer {
            width: 100%;
            height: 100%;
            show-player-layer: root.effective-show-player;
            video-surface-mode: root.player-video-surface-mode;
        }

        if (root.debug-preview-enabled) : AppDebugMenu {
            x: root.ui-unit;
            y: parent.height - self.height - root.ui-unit;
            unit: root.ui-unit;
            pages: root.debug-pages;
            states: root.debug-state-options;
            selected-page: root.debug-preview-page;
            selected-state: root.debug-preview-state;
            selected-style: root.ui-style;
            open <=> root.debug-menu-open;
            style-selected(style) => { root.ui-style = style; }
            page-selected(page) => {
                root.profile-menu-open = false;
                root.debug-preview-page = page;
                root.debug-preview-state = page == -1 ? "login"
                    : (page == 0 ? "loaded"
                    : (page == 1 ? "catalog"
                    : (page == 2 ? "loaded"
                    : (page == 3 ? "loaded"
                    : (page == 4 ? "general"
                    : (page == 5 ? "loaded"
                    : (page == 6 ? "results"
                    : (page == 7 ? "series-episodes" : "paused"))))))));
            }
            state-selected(state) => {
                root.profile-menu-open = root.debug-preview-page == 0 && state == "profile-menu";
                root.debug-preview-state = state;
                if (root.debug-preview-page == 8) {
                    root.player-controls-visible = state != "controls-hidden";
                    root.player-show-subtitles-menu = state == "subtitles";
                    root.player-show-audio-menu = state == "audio";
                    root.player-show-speed-menu = state == "speed";
                    root.player-show-stats-menu = state == "stats";
                    root.player-show-options-menu = state == "options";
                    root.player-show-context-menu = state == "context-menu";
                    root.player-show-playlist-drawer = state == "episodes";
                }
            }
        }
    }
}
