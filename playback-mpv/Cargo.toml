[package]
name = "playback-mpv"
version = "0.1.0"
edition = "2024"
links = "mpv"

[dependencies]
glow.workspace = true
thiserror.workspace = true

[build-dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
