[target.'cfg(all(windows, target_env = "msvc"))']
# Keep the Rust executable and its statically linked native crates on the static
# MSVC runtime. libmpv remains isolated behind its DLL C API boundary.
rustflags = ["-C", "target-feature=+crt-static"]
